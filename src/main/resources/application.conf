{
  // Address and port to the UDP endpoint
  source-endpoint {
    address = "localhost"
    port = 9000
  }
  // Output path on the local filesystem, only the last level could be created automatically
  output-path = "/home/castiel-vr/temp-upd/"
  // Kpi are calculated every ${events-grouping-size} events
  events-grouping-size = 1000
}
